# BE Data Schema

Backed by `BE/data/db.json`. See project root `SCHEMA.md` for full entity definitions.

**Tables:** parkingLots, parkingSpots, parking_spot_readings, historical_proxy_data, students, classes, class_schedule.
